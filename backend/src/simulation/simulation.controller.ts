import { Body, Controller, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { AuthGuard } from '../auth/jwt.guard'; import { CurrentUser } from '../common/current-user.decorator'; import { EventsGateway } from '../events.gateway'; import { SimulationDto } from './dto'; import { SimulationService } from './simulation.service';
@ApiTags('simulation') @ApiBearerAuth() @UseGuards(AuthGuard) @Controller('simulate') export class SimulationController{ constructor(private s:SimulationService, private events:EventsGateway){} @Post() async run(@CurrentUser() u,@Body() dto:SimulationDto){const log=await this.s.run(u.id,dto.device_id,dto.hours_used); this.events.emitToUser(u.id,'energy:created',log); this.events.emitToUser(u.id,'dashboard:updated',{action:'simulation_created'}); return log} }
